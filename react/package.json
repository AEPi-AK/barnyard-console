{
  "name": "barnyard-console",
  "version": "0.1.0",
  "private": true,
  "homepage": "./",
  "dependencies": {
    "rc-slider": "^7.0.0",
    "react": "^15.5.4",
    "react-dom": "^15.5.4",
    "react-json-syntax-highlighter": "^0.1.15",
    "react-native-slider": "^0.9.1"
  },
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "deploy": "react-scripts build && rm -rf ../app/build && mv build ../app/ && cd .. && git add app && git commit -m 'deploy to resin' && git push resin master",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
